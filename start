#!/bin/zsh
# Start local HTTP server and open the page
cd "$(dirname "$0")"
lsof -ti:8000 | xargs kill -9 2>/dev/null
python3 -m http.server 8000 &
sleep 1
open http://localhost:8000
echo "Server running at http://localhost:8000 (PID $!)"
